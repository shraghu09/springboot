name: Deploy API (java-api) to EC2

on:
  push:
    branches: [ "main" ]
    paths:
      - "java-api/**"
      - ".github/workflows/deploy-api.yml"

  workflow_dispatch:

jobs:
  deploy-api:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Build API App
        working-directory: java-api
        run: mvn clean package -DskipTests

      - name: Copy API Jar to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "java-api/target/*.jar"
          target: "/home/ubuntu/apps/api"

      - name: Restart API App in EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            pkill -f "java-api" || true
            nohup java -jar /home/ubuntu/apps/api/java-api/target/*.jar > /home/ubuntu/apps/api/api.log 2>&1 &
